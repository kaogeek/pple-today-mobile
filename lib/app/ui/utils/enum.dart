enum ModeType { email, mfp, act, facebook, twitter, google, apple }

enum ListTileType { delete, info }

enum TrimMode { length, line }

enum VoteChoiceType { single, multi, text }

enum AlertType { success, error, warning, info }
