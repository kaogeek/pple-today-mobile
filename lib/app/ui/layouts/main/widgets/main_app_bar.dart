import 'package:flutter/material.dart';

class MiainAppBar extends StatelessWidget {
  const MiainAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
